# Basic routing for BianconeriHub
Options -Indexes
RewriteEngine On

# Redirect all non-file requests to index.php for query-based routing
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
